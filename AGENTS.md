# このリポジトリでAIエージェントが行う作業

このファイルは、ChatGPT（Codex）などのAIエージェントがこのリポジトリを操作するときの指示書です。

このサイトはCloudflare Pagesで公開されている静的サイトです。`main` ブランチに入った内容が、そのまま本番サイト（https://www.andantino-shoes.jp/ ）になります。

## いちばん多い作業：記事を1本追加する

洋子さんから「この記事をサイトに載せて」と言われたときの手順です。**これ以外のファイルは触らないでください。**

### 手順

1. `content/articles/` に、Markdownファイルを1つ作る
2. `npm run build` を実行する
3. エラーが出たら直し、通るまで繰り返す
4. 生成された差分ごとコミットして push する

これだけです。HTMLファイルを手で書く必要はありません。`npm run build` が、記事ページ・一覧ページ・カテゴリ別ページ・sitemap をすべて自動で作り直します。

### ファイル名

**ファイル名がそのままURLになります。**

```
content/articles/shingakki-kodomo-kutsu-check.md
  ↓
https://www.andantino-shoes.jp/articles/shingakki-kodomo-kutsu
```

- 半角小文字・数字・ハイフンのみ（日本語・大文字・アンダースコアは不可）
- 内容がわかる英語かローマ字にする
- 一度公開したファイル名は変更しない（URLが変わると、検索結果からのリンクが切れるため）

### ファイルの書き方

先頭を `---` で挟んだ部分（frontmatter）に情報を書き、その下に本文をMarkdownで書きます。

```markdown
---
title: 新学期の前に、お子さまの靴を確認しませんか
category: children
published_at: 2026-08-20
excerpt: 夏休みのあいだに足は大きくなります。新学期が始まる前に、今の靴が合っているかを確かめる方法をまとめました。
call_to_action: children
---

本文をここから書きます。

## 見出しは ## から始める

段落は空行で区切ります。
```

#### frontmatter の項目

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| `title` | **必須** | 記事タイトル。ページの `<h1>` と `<title>` になる |
| `category` | 推奨 | 下の一覧から選ぶ。省略や未対応の値は `other` 扱い |
| `published_at` | 推奨 | `2026-08-20` の形式。一覧の並び順に使う。省略時は本日 |
| `excerpt` | 推奨 | 一覧カードと検索結果に出る説明文。80〜120字程度。省略時は本文から自動生成 |
| `updated_at` | 任意 | 記事を書き直したときに更新する |
| `seo_title` | 任意 | 検索結果用のタイトルを `title` と変えたいときだけ |
| `seo_description` | 任意 | 検索結果用の説明文を `excerpt` と変えたいときだけ |
| `featured_image` | 任意 | `./assets/foo.webp` の形式。**すでにリポジトリにある画像だけ**を指定する |
| `call_to_action` | 任意 | 記事末尾のボタン。省略時は `line` |
| `draft` | 任意 | `true` にすると公開されない（下書き保存） |

#### `category` に使える値

| 値 | 掲載される一覧ページ |
| --- | --- |
| `children` | 子どもの靴 |
| `adult` | 大人の靴 |
| `foot-problems` | 外反母趾・足の悩み |
| `insoles` | インソール |
| `shoe-wearing` | 靴の履き方・選び方 |
| `seasonal` | （記事一覧のみ） |
| `other` | （記事一覧のみ） |

#### `call_to_action` に使える値

`line` / `reservation` / `services` / `children` / `insoles`

### 本文に使える記法

対応しているのは次のものだけです。これ以外の記法（表・画像埋め込み・引用など）は、書いてもそのまま文字として表示されます。

```markdown
## 大見出し
### 中見出し

段落は空行で区切ります。同じ段落の中で改行しても、1つの段落になります。

- 箇条書き
- 箇条書き

1. 番号つき
2. 番号つき

**太字**

[サイト内リンク](../childrens-shoes)
[外部リンク](https://example.com/)
```

**見出しは必ず `##`（h2）から始めてください。** ページのタイトルがすでに `<h1>` になっているため、本文に `#`（h1）を書くと見出しの階層が飛び、サイト監査でエラーになります（`#` と書いた場合は自動的に h2 に直されます）。

サイト内リンクは `../` から始めます（記事ページは `/articles/` の中にあるため）。**拡張子 `.html` は付けません**（公開URLが拡張子なしのため）。`../childrens-shoes.html` のように書いても、生成時に自動で `.html` が外れます。

### 記事を修正・削除するとき

- **修正**：該当の `.md` ファイルを直し、`updated_at` を更新して `npm run build`
- **非公開に戻す**：frontmatter に `draft: true` を足して `npm run build`（HTMLが自動で削除されます）
- **完全に削除**：`.md` ファイルを削除して `npm run build`

いずれも `articles/*.html` を直接編集・削除しないでください。`npm run build` のたびに上書きされます。

## 記事以外の依頼について

記事の追加以外にも、次のような依頼は対応して構いません。

- 商品ページの写真を差し替える／追加する
- 既存ページの文章を直す（料金改定、営業時間の変更、説明の追記など）
- 取扱商品やセミナー情報を追加する

いずれの場合も、作業後に必ず `npm run build` を通してからコミットしてください。

### 変更の危険度（Lv1〜Lv4）

`docs/SITE_PLAYBOOK.md` の分類に従います。**触ってよい頻度が段階で決まっています。**

| Lv | 対象 | 触ってよい頻度 |
| --- | --- | --- |
| 🚨 **Lv1** | 公開済みURL・ファイル名・ドメイン | **変更しない**（サイト全面改修時のみ） |
| ⚠️ **Lv2** | `canonical` / `robots.txt` / `_headers` / `_redirects` / `noindex` / sitemap | **変更しない**（sitemapは `npm run build` が自動生成） |
| 🟡 **Lv3** | `<title>` / `<h1>` / `meta description` / 構造化データ（JSON-LD） / グローバルナビ | 数週間〜数ヶ月に1回。**一度に1〜2ページまで** |
| 🟢 **Lv4** | 本文テキスト・**新規記事の追加**・写真の差し替え・内部リンク・alt | **毎日・何度でも可** |

**記事の追加と、ページの文章・写真の更新はLv4です。恐れず進めてください。** 検索順位を落とす原因になるのは、Lv1〜Lv2を触ることと、大量のページを一度に書き換えることだけです。

### 一度にたくさん変えない

1回の作業で **20ページ以上のHTMLが変わる場合は、作業を分けてください。** 影響は2〜4週間遅れて検索順位に出るため、変更直後に問題がなくても安全とは言えません。

- 記事を1本追加する → 記事ページ＋一覧ページ数枚。問題ありません
- 全ページ共通の部分（ヘッダー・フッター・`styles.css`）を書き換える → 全ページに波及します。**開発担当者に相談してください**

### 「SEOに影響はありません」と言わない

変更を説明するときは、影響がないと断定しないでください。低リスクだと考える場合も、**なぜ限定的なのかを具体的に書いてください。**

### 判断に迷ったら進めない

次のような依頼は、影響範囲が広く、壊れても画面上すぐには気づきにくい種類の変更です。作業を進める前に、開発担当者に相談するよう洋子さんに伝えてください。

- サイト全体のデザイン・レイアウトの変更（`styles.css` の大きな修正）
- 新しいページの追加（`scripts/site-pages.mjs` への登録が必要になります）
- ナビゲーションメニューの構成変更（`script.js` と `index.html` の両方を揃える必要があります）
- `scripts/` 以下の生成スクリプトの変更

### 必ず `npm run build` を通す

コミット前に必ず実行し、成功することを確認してください。

```bash
npm run build
```

このコマンドには `scripts/audit-site.mjs` によるサイト監査（78項目）が含まれています。メタ情報の重複、見出し階層の誤り、リンク切れ、sitemapとの不整合などがあると、ここで失敗します。**失敗したままコミットしないでください。**

### 事実を創作しない

このサイトは、認定シューフィッターである五十嵐洋子の名前で公開される専門的な内容です。記事は洋子さん本人の文章と経験にもとづきます。

- 洋子さんが言っていない専門的判断・数値・効果を書き足さない
- 医学的な断定（「治ります」「予防できます」など）はしない
- 料金・営業時間・サービス内容を、勝手に変更・追記しない

内容に不足があると感じた場合は、書き足すのではなく、洋子さんに確認してください。

## 参考

- **変更の危険度・運用ルールの詳細：`docs/SITE_PLAYBOOK.md`**（Lv1〜4の分類、爆発半径、デプロイ前チェック、事故対応）
- 文章のトーン：`docs/VOICE_GUIDE.md`
- サイト全体の運用：`README.md`
- 書式の見本：`content/articles/` にある既存ファイル
